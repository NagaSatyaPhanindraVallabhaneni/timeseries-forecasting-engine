# torch resolves to the CPU wheel via the extra index below (avoids the ~800MB CUDA wheel)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
fastapi>=0.110
uvicorn>=0.29
numpy>=1.26
pydantic>=2.6
pytest>=8
httpx>=0.27
ruff>=0.4
